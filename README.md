# git_HandsOn

This repository contains a simple Python script called `seqClass.py`.

The script classifies an input sequence as DNA or RNA and can optionally search for a motif.

Usage:

```bash
python seqClass.py -s ACTG
python seqClass.py -s ACTG -m TG
```text
Add README
